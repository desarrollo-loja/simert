import { Injectable } from '@nestjs/common';

/**
 * Authentication service placeholder. Authentication logic is handled by
 * {@link KeycloakService} and the `KeycloakTokenGuard` guard; this class
 * is reserved for future local auth extensions.
 */
@Injectable()
export class AuthService {}
